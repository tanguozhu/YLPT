@model IEnumerable<WebApplication4.Models.Previouseradication>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Radicationtime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Radicationdate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Radicationcase)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Course)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Result)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Radicationtime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Radicationdate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Radicationcase)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Course)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Result)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
