@model WebApplication4.Models.BriefIntro

@{
    ViewBag.Title = "Edit";
}

<h2>编辑</h2>


@using (Html.BeginForm("Edit", "BriefIntroes", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    
<div class="form-horizontal">
    <h4>科室简介</h4>
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    @Html.HiddenFor(model => model.Id)

    <div class="form-group">
        <label class="control-label col-md-2" for="Introduction">内容</label>
        <div class="col-md-10">
            <textarea cols="20" htmlattributes="{ class = white-space:pre-wrap }" style="height:200px;width:400px" id="Introduction" name="Introduction" rows="2">@Model.Introduction</textarea>
            @Html.ValidationMessageFor(model => model.Introduction, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        <label class="control-label col-md-2">选择图片</label>
        <div class="col-md-10">
            <input type="file" name="filename" id="filename" value="选择" />
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-10" style="display:none">
            @Html.EditorFor(model => model.Pic, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.Pic, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="保存" class="btn btn-default" />
        </div>
    </div>
</div>
}

<div style="font-size:20px">
    @Html.ActionLink("返回科室简介主页", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
